# Copy to .env (gitignored). Fill as integrations are wired up.

# Google Drive access for pulling source files (RMI xlsx, China tracker sheet).
# Preferred path is the claude.ai Drive MCP; a service-account key is only needed for
# headless/cron ingest.
# GOOGLE_APPLICATION_CREDENTIALS=

# Entity lookup / a future GRT backend, if/when one exists.
# GRT_DB_URL=
