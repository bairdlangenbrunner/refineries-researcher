# Source roster

Background datasets registered under `sources/`, plus preferred independent research
sources. **None of the GEM-authored datasets is citable** — they are seed/reconciliation
data. `source_tier` drives confidence (see `confidence_tiers.md`).

## Registered background datasets (`sources/<name>/manifest.yml`)

Row counts below are ingested canonical rows (see each `sources/<name>/canonical_summary.json`).

| name | What it is | Rows | Key fields | Tier | Notes |
|---|---|---|---|---|---|
| `rmi` | RMI Refinery List (February '23) | 484 worldwide | id, name, design capacity (bbl/day), country, ISO3, lat/lon, WKT, type (H/M/D), typology source | 2 | Primary global seed; near-full capacity + ISO3 + configuration, **no status column**, cap `0`/`<=0` = unknown/idle. Drive: `Copy of RMI Refinery List (February '23).xlsx` in folder `1gJaJ7KYByNAHzoF0ve6ienuEvsfP28bV` (fileId `15CoiFuiT-JtDD-cb3ZV-Cx_Qb8RUPiDk`). Many `Typology Source` cells are `abarrelfull.wikidot.com` — **not citable**, chase the primary. |
| `ogj` | Oil & Gas Journal — Worldwide Refining Survey PDF | 577 worldwide (105 countries) | owner, city, Crude b/cd (→kbpd), country/ISO3; full downstream-unit breakdown in-PDF | 2 | Rebuilt from the **WW Refining PDF** (`adapter.py`, pdfplumber): country on EVERY row, Crude distillation → kbpd. **No coordinates** (trade-off vs the old Europe-only map JSON, preserved as `adapter_mapjson.py`). Reconciles with per-section PDF Totals bar 3 documented quirks. Because it's coordless, it dedups via the country-blocked greedy-1:1 path in `match.py`. |
| `ogim` | OGIM v2.7 `Crude_Oil_Refineries` layer | 692 worldwide | GIS points; design + throughput bpd, location-focused | 2 | **Location source**: coords 100%, capacity 600/692 (`-999` sentinel nulled) — but status 79% `N/A` and start-year 98% `1900` sentinel, so weak on status/vintage. `SRC_REF_ID` is an ID into OGIM's own catalog, not a URL. gpkg at `/Users/baird/Dropbox/_gis-data/ogim/OGIM_v2.7.gpkg` (3 GB, read single layer). |
| `eia` | EIA Refinery Capacity Report (Form EIA-820), `refcap{YY}.xlsx` | 124 (**US only**, crude) | owner, site/city, state, PADD, atmospheric crude distillation (b/cd→kbpd), operable/idle status; downstream-unit slate in-file; lat/lon joined from EIA GIS | 1 | **Primary US-gov source, the US capacity gold standard.** Long/tidy workbook (one row per refinery×PRODUCT×SUPPLY) → adapter pivots on `(CORPORATION,COMPANY_NAME,STATE_NAME,SITE)`; capacity = `TOTAL OPERABLE CAPACITY` b/cd; status DERIVED from OPERATING/IDLE (not a column). Coords left-joined from the EIA Energy Atlas GIS layer (2021 vintage → 120/124 join; rest null, not fabricated). **Excludes 6 downstream-only petchem/lube/NGL sites** (no crude distillation) — listed in `canonical_summary.json:excluded_out_of_scope`. Mergeable (unlike irs_rcn). Public domain, citable. Naming: `refcap{YY}.xlsx`; GIS `Petroleum_Refineries_US_EIA.zip`. |
| `india_ppac` | India PPAC "Installed Refining Capacity" (1 Apr 2025) | 23 (**India only**) | operator, refinery, state, capacity ('000 MT/yr → kbpd); no coords/status | 1 | **Primary India-gov source** (Petroleum Planning & Analysis Cell, MoP&NG). Single xlsx; adapter forward-fills the vertically-merged company column, drops the total/footnote rows. ⚠ UNIT `'000 MT`/yr (`kt/a`), 1000× the MMTPA figure (Panipat 15000 = 15 MMTPA ≈ 301 kbpd) — new conversion in `capacity_normalize`. RIL Jamnagar is TWO co-located rows (DTA + SEZ). Cauvery Basin listed at 0 → null cap. Coordless → country-blocked greedy-1:1 match. Citable `.gov.in`. |
| `brazil_anp` | Brazil ANP Anuário Estatístico 2025, Table 2.29 (as of 31/12/2024) | 18 (**Brazil only**) | refinery, município/UF, start year, nominal capacity (bbl/day → kbpd); no operator/coords/status | 1 | **Primary Brazil-gov source** (ANP, the petroleum regulator). `;`-CSV, UTF-8-BOM (`utf-8-sig`), comma decimal. Capacity is **bbl/day** (REPLAN 433,996 = 434 kbpd), NOT m³. No operator column (owner implicit in name; post-divestiture operators like Acelen/3R absent). 'Paraná Xisto' (SIX oil-shale, 0 crude bbl/d) emitted null-cap → **RULING (Baird): IN SCOPE** (GORT tracks all refineries, not only crude). Coordless → country-blocked match. Surfaced 1 genuine miss: Ssoil Energy (Coroados SP, 12.5 kbpd, 2021). Citable federal open data. |
| `climate_trace` | Climate TRACE `oil-and-gas-refining` asset layer (v5.8.0, Jun '26) | 728 worldwide (110 countries) | stable Id, name, ISO3, point lat/lon, max refining capacity (bbl/day → kbpd), process type→config, owner (+CompanyId/LEI); **no status, no start year** | 2 | **Independent worldwide coord+capacity+config source** (not GEM → citable, CC BY 4.0). Pulled from the v6 REST API in one call (bulk bucket only ships all-sector country zips). `AssetType` prefix-maps to Configuration (no `topping`). Capacity is **nameplate max** in clean bbl/day (no unit trap) → runs high vs OGJ/RMI operating figures (conflict, not adopt). Coordinate-bearing → cKDTree match path. Surfaced 3 world-scale genuine misses (Dangote NGA 650, Pemex Olmeca MEX 340, Duqm OMN 230). Only operating assets (728 vs RMI ~800). |
| `china_rmi_tracker` | GEM China Independent Oil Refinery Tracker for RMI | 101 (China) | full GEM `[ref]`-paired schema + data dictionary | — | **Schema template** for the whole project + China seed data; fully populated, `tttpa`→kbpd verified to 0.1% vs the sheet's own column. GEM-authored → **seed only, never a citation.** Sibling tab "Extra facilities (out of scope)" (43 rows) is a Phase-B scope signal. Drive: Google Sheet `1PyNUtGUDLdY1chJ-MkzzgV_OnAcNTp2QlIq8jLhStPw`. No Country column → ingest stamps `defaults: {country: China, iso3: CHN}`. |
| `irs_rcn` | IRS "Active Fuel Refineries" (Refiner Control Number) registry PDF | 227 (**US only**) | RCN id, facility name, state (from RCN), city; **no capacity/coords/status/config** | 1 | Primary US-gov registry (IRC §4101 Form 637 "S"). ⚠ **Tax definition, not crude-only** — ~half the rows are gas/NGL/renewable-diesel/LNG/petchem. Coordless + capacityless → the capacity-gated matcher NEVER auto-matches it; every pair is `possible`. **RULING (Baird): OVERLAY ONLY — never merged into the master;** sole use is the reconciliation/discovery workbook `batches/refineries_irs_reconciliation_*.xlsx`. `url`: `irs.gov/pub/irs-sbse/rcn-db.pdf`. "Active" = active registration, not operating status. |

## Preferred independent research sources (for `[ref]` corroboration)

- Company/operator sites, annual reports, investor decks (primary).
- National regulators / energy ministries / statistics agencies (primary).
- EIA, JODI, OPEC, IEA reports (secondary, authoritative).
- Reputable trade press: Reuters, Argus, S&P Global (Platts), Hydrocarbon Processing,
  Oil & Gas Journal articles, Upstream, hydrocarbons-technology.com.
- **Not independent / not citable:** gem.wiki, globalenergymonitor.org, anything tracing
  to GEM, `abarrelfull.wikidot.com` (community wiki that echoes GEM/OGJ — chase its
  footnote), Wikipedia when it merely footnotes one of the above (cite the footnote).
